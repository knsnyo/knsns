import { useChangeUser } from './change'
import { useGetUser } from './get'
import { useGetFollowUsers } from './get-follow-users'

export const api = {
	useGetUser,
	useChangeUser,
	useGetFollowUsers
}
